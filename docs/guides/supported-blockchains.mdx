import DataTable from "../../src/components/DataTable/DataTable";
import {supportedBlockchains, supportedTestnetRelayChains, columns} from "./supported-blockchains.table-data";

The following table shows the full list of supported, reward-generating RelayChains

<DataTable
    columns={columns}
    data={supportedBlockchains}
/>

## Pocket Testnet RelayChains

The following table shows the full list of currently-supported blockchains on the Pocket Testnet.

<DataTable
    columns={columns}
    data={supportedTestnetRelayChains}
/>